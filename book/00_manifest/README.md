# Chapter manifest

This directory stores chapter workflow metadata for the manual chapter workflow.

Use `chapter-status.schema.json` to validate chapter status files and `chapter-status.example.json` as a minimal example of a tracked chapter.

The manifest tracks only processed project artifacts and paths. It must not contain raw source text, long quotations, or raw book files.

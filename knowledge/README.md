# Knowledge Base

This directory is the baseline structure for project knowledge sources.

- `00_manifest/` keeps source registration, priority, and upload rules.
- `01_raw_private/` is reserved for private raw materials and should not be committed except for its README placeholder.
- `04_processed/source_cards/` stores normalized source-card templates and processed summaries.
